public enum MaskOrigin
{
    Center = 0,

    LeftOrTop = 1,
    RightOrBottom = 2,

    // Convenience aliases
    Left = LeftOrTop,
    Top = LeftOrTop,
    Right = RightOrBottom,
    Bottom = RightOrBottom
}