[gd_scene format=3 uid="uid://snfu2b1ifqye"]

[ext_resource type="Script" uid="uid://biye3d0dqqeuj" path="res://ShapeGame.cs" id="1_ryrav"]
[ext_resource type="PackedScene" uid="uid://bw1n7eocfhsbo" path="res://entity/living/player/player.tscn" id="2_eow3j"]
[ext_resource type="PackedScene" uid="uid://dlwvs3u6gqtsw" path="res://ui/game/game_ui.tscn" id="3_qhcbd"]
[ext_resource type="PackedScene" uid="uid://bliriv38qvb4i" path="res://shop/shop.tscn" id="5_e8heu"]
[ext_resource type="Script" uid="uid://cbwxymx4i00f0" path="res://environment/Environment.cs" id="5_f1jbl"]
[ext_resource type="PackedScene" uid="uid://csitvuhpytc2q" path="res://ui/inventory/inventory_manager.tscn" id="5_hve3p"]
[ext_resource type="PackedScene" uid="uid://b063yya1e0eik" path="res://ui/inventory/pause/pause_menu.tscn" id="6_mfdv2"]
[ext_resource type="PackedScene" uid="uid://cjmguojhwcatt" path="res://debug/screen/debug_screen.tscn" id="6_rvswv"]
[ext_resource type="PackedScene" uid="uid://dy2k3kjbjir5i" path="res://ui/game_over/game_over_screen.tscn" id="7_hb5m1"]
[ext_resource type="PackedScene" uid="uid://c1qxnkasfw75n" path="res://util/glow/rect_glow.tscn" id="7_vaj68"]
[ext_resource type="Script" uid="uid://djxx4y1127mi" path="res://ui/FpsCounter.cs" id="8_215e1"]
[ext_resource type="Script" uid="uid://fxme35ooeqpu" path="res://level/LevelManager.cs" id="8_j5wjh"]
[ext_resource type="Script" uid="uid://6vqg7uvcvpn2" path="res://entity/living/enemies/EnemyManager.cs" id="8_vef74"]
[ext_resource type="PackedScene" uid="uid://b8gd2gve05upa" path="res://systems/CrystalManager.tscn" id="9_vef74"]
[ext_resource type="Texture2D" uid="uid://ck75mef3o2t0i" path="res://environment/background_square.png" id="10_hve3p"]
[ext_resource type="Script" uid="uid://dbtjcecl2kyym" path="res://entity/spawnables/static/explosion/ExplosionEffects.cs" id="11_mfdv2"]
[ext_resource type="Shader" uid="uid://c8tu4dwq8epsu" path="res://util/screenshake/screen_shake.gdshader" id="12_iotsf"]
[ext_resource type="Script" uid="uid://bd0itqdn7nlo7" path="res://systems/GamePhaseManager.cs" id="13_80cx4"]
[ext_resource type="Script" uid="uid://cek6yfy2gipxe" path="res://util/screenshake/ScreenShake.cs" id="13_lc2xo"]
[ext_resource type="Script" uid="uid://di80l6kh6f4gs" path="res://module/world/WorldModuleManager.cs" id="14_boyg6"]
[ext_resource type="Script" uid="uid://bhctivvx8cd75" path="res://systems/ScreenManager.cs" id="15_e36ub"]
[ext_resource type="Script" uid="uid://d04nmdt4omsif" path="res://systems/MouseInputManager.cs" id="16_boyg6"]
[ext_resource type="Script" uid="uid://ca64700tbqs0h" path="res://entity/living/player/PlayerManager.cs" id="17_x5p1p"]
[ext_resource type="Script" uid="uid://dcfyc3mh3ufim" path="res://debug/DebugDraw.cs" id="19_boyg6"]
[ext_resource type="Script" uid="uid://cncdoym5y3n6m" path="res://entity/spawnables/SpawnableManager.cs" id="19_vaj68"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_87y3e"]
size = Vector2(1024, 4208)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_0lhi4"]
size = Vector2(1024, 4208)

[sub_resource type="Shader" id="Shader_f1jbl"]
resource_local_to_scene = true
code = "shader_type canvas_item;
render_mode unshaded;

uniform vec4 glow_color : source_color = vec4(0.937, 0.357, 0.357, 1.0);
uniform float glow_radius : hint_range(0, 200) = 12.0;
uniform float glow_strength : hint_range(0, 10) = 2.0;

// Size of the ColorRect in local pixels. Set from RectGlow, replaces texture size.
uniform vec2 rect_size = vec2(1.0);

varying vec4 vertex_color;

// Standard normal CDF, logistic approximation. Smooth everywhere, cheap, ~1% error.
float ndf_cdf(float z) {
	return 1.0 / (1.0 + exp(-1.702 * z));
}

// How far past the rectangle edge the halo needs to reach before it fades to ~0.
// Grows with the blur sigma and, logarithmically, with strength (which lifts the tail).
float glow_extent(float sigma) {
	return sigma * (3.24 + 0.588 * log(max(glow_strength, 1.0)));
}

void vertex() {
	vertex_color = COLOR;
	float sigma = glow_radius * 0.35;
	VERTEX += (UV - 0.5) * 2.0 * glow_extent(sigma);
}

void fragment() {
	if (glow_radius < 0.5 || glow_strength < 0.01) {
		discard;
	}
	float sigma = glow_radius * 0.35;
	float extent = glow_extent(sigma);

	vec2 pixel_size = 1.0 / rect_size;
	vec2 expanded_scale = vec2(1.0) + 2.0 * extent * pixel_size;
	vec2 uv = (UV - 0.5) * expanded_scale + 0.5;

	// Gaussian blur of a solid rectangle is separable: the coverage at a point is
	// the product of two 1D edge integrals. Smooth fill inside, soft falloff outside,
	// no banding, no texture samples.
	vec2 pos = uv * rect_size;
	float inv_sigma = 1.0 / sigma;
	float coverage_x = ndf_cdf(pos.x * inv_sigma) - ndf_cdf((pos.x - rect_size.x) * inv_sigma);
	float coverage_y = ndf_cdf(pos.y * inv_sigma) - ndf_cdf((pos.y - rect_size.y) * inv_sigma);
	float coverage = coverage_x * coverage_y;

	// strength shapes the outer halo (1 = plain gaussian blur, >1 = brighter/wider glow),
	// while the inside stays a solid fill.
	float alpha = (1.0 - pow(1.0 - coverage, glow_strength)) * glow_color.a * vertex_color.a;

	// Feather the very edge of the expanded quad so the halo can never cut off sharply.
	vec2 edge = min(UV, 1.0 - UV);
	alpha *= smoothstep(0.0, 0.02, min(edge.x, edge.y));

	COLOR = vec4(glow_color.rgb * vertex_color.rgb, alpha);
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_vaj68"]
resource_local_to_scene = true
shader = SubResource("Shader_f1jbl")
shader_parameter/glow_color = Color(0.22352941, 0.627451, 0.92941177, 1)
shader_parameter/glow_radius = 96.00000456
shader_parameter/glow_strength = 2.000000095
shader_parameter/rect_size = Vector2(1024, 4208)

[sub_resource type="Shader" id="Shader_i2p71"]
resource_local_to_scene = true
code = "shader_type canvas_item;
render_mode unshaded;

uniform vec4 glow_color : source_color = vec4(0.937, 0.357, 0.357, 1.0);
uniform float glow_radius : hint_range(0, 200) = 12.0;
uniform float glow_strength : hint_range(0, 10) = 2.0;

// Size of the ColorRect in local pixels. Set from RectGlow, replaces texture size.
uniform vec2 rect_size = vec2(1.0);

varying vec4 vertex_color;

// Standard normal CDF, logistic approximation. Smooth everywhere, cheap, ~1% error.
float ndf_cdf(float z) {
	return 1.0 / (1.0 + exp(-1.702 * z));
}

// How far past the rectangle edge the halo needs to reach before it fades to ~0.
// Grows with the blur sigma and, logarithmically, with strength (which lifts the tail).
float glow_extent(float sigma) {
	return sigma * (3.24 + 0.588 * log(max(glow_strength, 1.0)));
}

void vertex() {
	vertex_color = COLOR;
	float sigma = glow_radius * 0.35;
	VERTEX += (UV - 0.5) * 2.0 * glow_extent(sigma);
}

void fragment() {
	if (glow_radius < 0.5 || glow_strength < 0.01) {
		discard;
	}
	float sigma = glow_radius * 0.35;
	float extent = glow_extent(sigma);

	vec2 pixel_size = 1.0 / rect_size;
	vec2 expanded_scale = vec2(1.0) + 2.0 * extent * pixel_size;
	vec2 uv = (UV - 0.5) * expanded_scale + 0.5;

	// Gaussian blur of a solid rectangle is separable: the coverage at a point is
	// the product of two 1D edge integrals. Smooth fill inside, soft falloff outside,
	// no banding, no texture samples.
	vec2 pos = uv * rect_size;
	float inv_sigma = 1.0 / sigma;
	float coverage_x = ndf_cdf(pos.x * inv_sigma) - ndf_cdf((pos.x - rect_size.x) * inv_sigma);
	float coverage_y = ndf_cdf(pos.y * inv_sigma) - ndf_cdf((pos.y - rect_size.y) * inv_sigma);
	float coverage = coverage_x * coverage_y;

	// strength shapes the outer halo (1 = plain gaussian blur, >1 = brighter/wider glow),
	// while the inside stays a solid fill.
	float alpha = (1.0 - pow(1.0 - coverage, glow_strength)) * glow_color.a * vertex_color.a;

	// Feather the very edge of the expanded quad so the halo can never cut off sharply.
	vec2 edge = min(UV, 1.0 - UV);
	alpha *= smoothstep(0.0, 0.02, min(edge.x, edge.y));

	COLOR = vec4(glow_color.rgb * vertex_color.rgb, alpha);
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xa8c5"]
resource_local_to_scene = true
shader = SubResource("Shader_i2p71")
shader_parameter/glow_color = Color(0.22352941, 0.627451, 0.92941177, 1)
shader_parameter/glow_radius = 96.00000456
shader_parameter/glow_strength = 2.000000095
shader_parameter/rect_size = Vector2(1024, 4208)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_k07pp"]
size = Vector2(3840, 1024)

[sub_resource type="Shader" id="Shader_5b14r"]
resource_local_to_scene = true
code = "shader_type canvas_item;
render_mode unshaded;

uniform vec4 glow_color : source_color = vec4(0.937, 0.357, 0.357, 1.0);
uniform float glow_radius : hint_range(0, 200) = 12.0;
uniform float glow_strength : hint_range(0, 10) = 2.0;

// Size of the ColorRect in local pixels. Set from RectGlow, replaces texture size.
uniform vec2 rect_size = vec2(1.0);

varying vec4 vertex_color;

// Standard normal CDF, logistic approximation. Smooth everywhere, cheap, ~1% error.
float ndf_cdf(float z) {
	return 1.0 / (1.0 + exp(-1.702 * z));
}

// How far past the rectangle edge the halo needs to reach before it fades to ~0.
// Grows with the blur sigma and, logarithmically, with strength (which lifts the tail).
float glow_extent(float sigma) {
	return sigma * (3.24 + 0.588 * log(max(glow_strength, 1.0)));
}

void vertex() {
	vertex_color = COLOR;
	float sigma = glow_radius * 0.35;
	VERTEX += (UV - 0.5) * 2.0 * glow_extent(sigma);
}

void fragment() {
	if (glow_radius < 0.5 || glow_strength < 0.01) {
		discard;
	}
	float sigma = glow_radius * 0.35;
	float extent = glow_extent(sigma);

	vec2 pixel_size = 1.0 / rect_size;
	vec2 expanded_scale = vec2(1.0) + 2.0 * extent * pixel_size;
	vec2 uv = (UV - 0.5) * expanded_scale + 0.5;

	// Gaussian blur of a solid rectangle is separable: the coverage at a point is
	// the product of two 1D edge integrals. Smooth fill inside, soft falloff outside,
	// no banding, no texture samples.
	vec2 pos = uv * rect_size;
	float inv_sigma = 1.0 / sigma;
	float coverage_x = ndf_cdf(pos.x * inv_sigma) - ndf_cdf((pos.x - rect_size.x) * inv_sigma);
	float coverage_y = ndf_cdf(pos.y * inv_sigma) - ndf_cdf((pos.y - rect_size.y) * inv_sigma);
	float coverage = coverage_x * coverage_y;

	// strength shapes the outer halo (1 = plain gaussian blur, >1 = brighter/wider glow),
	// while the inside stays a solid fill.
	float alpha = (1.0 - pow(1.0 - coverage, glow_strength)) * glow_color.a * vertex_color.a;

	// Feather the very edge of the expanded quad so the halo can never cut off sharply.
	vec2 edge = min(UV, 1.0 - UV);
	alpha *= smoothstep(0.0, 0.02, min(edge.x, edge.y));

	COLOR = vec4(glow_color.rgb * vertex_color.rgb, alpha);
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_bnpwn"]
resource_local_to_scene = true
shader = SubResource("Shader_5b14r")
shader_parameter/glow_color = Color(0.22352941, 0.627451, 0.92941177, 1)
shader_parameter/glow_radius = 96.00000456
shader_parameter/glow_strength = 0.0
shader_parameter/rect_size = Vector2(3840, 1024)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_jkk55"]
size = Vector2(3840, 1024)

[sub_resource type="Shader" id="Shader_36jdj"]
resource_local_to_scene = true
code = "shader_type canvas_item;
render_mode unshaded;

uniform vec4 glow_color : source_color = vec4(0.937, 0.357, 0.357, 1.0);
uniform float glow_radius : hint_range(0, 200) = 12.0;
uniform float glow_strength : hint_range(0, 10) = 2.0;

// Size of the ColorRect in local pixels. Set from RectGlow, replaces texture size.
uniform vec2 rect_size = vec2(1.0);

varying vec4 vertex_color;

// Standard normal CDF, logistic approximation. Smooth everywhere, cheap, ~1% error.
float ndf_cdf(float z) {
	return 1.0 / (1.0 + exp(-1.702 * z));
}

// How far past the rectangle edge the halo needs to reach before it fades to ~0.
// Grows with the blur sigma and, logarithmically, with strength (which lifts the tail).
float glow_extent(float sigma) {
	return sigma * (3.24 + 0.588 * log(max(glow_strength, 1.0)));
}

void vertex() {
	vertex_color = COLOR;
	float sigma = glow_radius * 0.35;
	VERTEX += (UV - 0.5) * 2.0 * glow_extent(sigma);
}

void fragment() {
	if (glow_radius < 0.5 || glow_strength < 0.01) {
		discard;
	}
	float sigma = glow_radius * 0.35;
	float extent = glow_extent(sigma);

	vec2 pixel_size = 1.0 / rect_size;
	vec2 expanded_scale = vec2(1.0) + 2.0 * extent * pixel_size;
	vec2 uv = (UV - 0.5) * expanded_scale + 0.5;

	// Gaussian blur of a solid rectangle is separable: the coverage at a point is
	// the product of two 1D edge integrals. Smooth fill inside, soft falloff outside,
	// no banding, no texture samples.
	vec2 pos = uv * rect_size;
	float inv_sigma = 1.0 / sigma;
	float coverage_x = ndf_cdf(pos.x * inv_sigma) - ndf_cdf((pos.x - rect_size.x) * inv_sigma);
	float coverage_y = ndf_cdf(pos.y * inv_sigma) - ndf_cdf((pos.y - rect_size.y) * inv_sigma);
	float coverage = coverage_x * coverage_y;

	// strength shapes the outer halo (1 = plain gaussian blur, >1 = brighter/wider glow),
	// while the inside stays a solid fill.
	float alpha = (1.0 - pow(1.0 - coverage, glow_strength)) * glow_color.a * vertex_color.a;

	// Feather the very edge of the expanded quad so the halo can never cut off sharply.
	vec2 edge = min(UV, 1.0 - UV);
	alpha *= smoothstep(0.0, 0.02, min(edge.x, edge.y));

	COLOR = vec4(glow_color.rgb * vertex_color.rgb, alpha);
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_xfvwf"]
resource_local_to_scene = true
shader = SubResource("Shader_36jdj")
shader_parameter/glow_color = Color(0.22352941, 0.627451, 0.92941177, 1)
shader_parameter/glow_radius = 96.00000456
shader_parameter/glow_strength = 0.0
shader_parameter/rect_size = Vector2(3840, 1024)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_j5wjh"]
size = Vector2(5888, 1024)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_7sc4i"]
size = Vector2(1024, 6256)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_80cx4"]
size = Vector2(1024, 6256.25)

[sub_resource type="RectangleShape2D" id="RectangleShape2D_vef74"]
size = Vector2(5888, 1024)

[sub_resource type="ShaderMaterial" id="ShaderMaterial_lc2xo"]
shader = ExtResource("12_iotsf")
shader_parameter/strength = 0.0
shader_parameter/magnitude = Vector2(0, 0)

[node name="Game" type="Node2D" unique_id=1682739906]
script = ExtResource("1_ryrav")

[node name="Background" type="CanvasLayer" parent="." unique_id=1099077609]
layer = -2

[node name="Parallax2D" type="Parallax2D" parent="Background" unique_id=1419804882]
z_index = -2
repeat_size = Vector2(724, 724)
autoscroll = Vector2(0, 100)
repeat_times = 6
follow_viewport = false
ignore_camera_scroll = true

[node name="Sprite2D" type="Sprite2D" parent="Background/Parallax2D" unique_id=1231804813]
position = Vector2(1920, 1080)
texture = ExtResource("10_hve3p")

[node name="BackgroundUI" type="CanvasLayer" parent="." unique_id=479357002]
layer = -1

[node name="LevelProgress" parent="BackgroundUI" unique_id=676634815 instance=ExtResource("3_qhcbd")]

[node name="Player" parent="." unique_id=692351150 instance=ExtResource("2_eow3j")]
position = Vector2(2174, 1930)

[node name="Environment" type="Node2D" parent="." unique_id=1509415066]
script = ExtResource("5_f1jbl")

[node name="LevelWalls" type="StaticBody2D" parent="Environment" unique_id=1246014687 groups=["level_walls"]]
collision_layer = 32
collision_mask = 0

[node name="LeftWall" type="CollisionShape2D" parent="Environment/LevelWalls" unique_id=1024175451]
position = Vector2(-512, 1080)
shape = SubResource("RectangleShape2D_87y3e")

[node name="RightWall" type="CollisionShape2D" parent="Environment/LevelWalls" unique_id=1028911608]
position = Vector2(4352, 1080)
shape = SubResource("RectangleShape2D_0lhi4")

[node name="LeftGlow" parent="Environment/LevelWalls" unique_id=1402074923 instance=ExtResource("7_vaj68")]
material = SubResource("ShaderMaterial_vaj68")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = -1024.0
offset_top = -1024.0
offset_right = 0.0
offset_bottom = 3184.0
grow_horizontal = 2
grow_vertical = 2

[node name="RightGlow" parent="Environment/LevelWalls" unique_id=312963737 instance=ExtResource("7_vaj68")]
material = SubResource("ShaderMaterial_xa8c5")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = 3840.0
offset_top = -1024.0
offset_right = 4864.0
offset_bottom = 3184.0
grow_horizontal = 2
grow_vertical = 2

[node name="LevelCeiling" type="StaticBody2D" parent="Environment" unique_id=1646188548]
collision_layer = 64
collision_mask = 0

[node name="TopWall" type="CollisionShape2D" parent="Environment/LevelCeiling" unique_id=467383974]
position = Vector2(1920, -512)
shape = SubResource("RectangleShape2D_k07pp")

[node name="CeilingGlow" parent="Environment/LevelCeiling" unique_id=1382008011 instance=ExtResource("7_vaj68")]
material = SubResource("ShaderMaterial_bnpwn")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_top = -1024.0
offset_right = 3840.0
offset_bottom = 0.0
grow_horizontal = 2
grow_vertical = 2

[node name="LevelFloor" type="StaticBody2D" parent="Environment" unique_id=798973780]
collision_layer = 128
collision_mask = 0

[node name="BottomWall" type="CollisionShape2D" parent="Environment/LevelFloor" unique_id=1878348994]
position = Vector2(1920, 2672)
shape = SubResource("RectangleShape2D_jkk55")

[node name="FloorGlow" parent="Environment/LevelFloor" unique_id=1794150407 instance=ExtResource("7_vaj68")]
material = SubResource("ShaderMaterial_xfvwf")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
offset_top = 2160.0
offset_right = 3840.0
offset_bottom = 3184.0
grow_horizontal = 2
grow_vertical = 2

[node name="LevelOutsideBoundary" type="StaticBody2D" parent="Environment" unique_id=345506684]
collision_layer = 256
collision_mask = 0

[node name="TopWall" type="CollisionShape2D" parent="Environment/LevelOutsideBoundary" unique_id=1615479064]
position = Vector2(1920, -1536)
shape = SubResource("RectangleShape2D_j5wjh")
debug_color = Color(0.294118, 0.615686, 0.384314, 0.419608)

[node name="LeftWall" type="CollisionShape2D" parent="Environment/LevelOutsideBoundary" unique_id=378929884]
position = Vector2(-1536, 1080)
shape = SubResource("RectangleShape2D_7sc4i")
debug_color = Color(0.294118, 0.615686, 0.384314, 0.419608)

[node name="RightWall" type="CollisionShape2D" parent="Environment/LevelOutsideBoundary" unique_id=681198289]
position = Vector2(5376, 1079.875)
shape = SubResource("RectangleShape2D_80cx4")
debug_color = Color(0.294118, 0.615686, 0.384314, 0.419608)

[node name="BottomWall" type="CollisionShape2D" parent="Environment/LevelOutsideBoundary" unique_id=188552543]
position = Vector2(1920, 3696)
shape = SubResource("RectangleShape2D_vef74")
debug_color = Color(0.294118, 0.615686, 0.384314, 0.419608)

[node name="Shop" parent="." unique_id=1220203929 instance=ExtResource("5_e8heu")]
z_index = -1
metadata/_edit_lock_ = true

[node name="UI" type="CanvasLayer" parent="." unique_id=1677117807]

[node name="InventoryManager" parent="UI" unique_id=338909771 instance=ExtResource("5_hve3p")]

[node name="GameOverScreen" parent="UI" unique_id=421136395 instance=ExtResource("7_hb5m1")]

[node name="PauseMenu" parent="UI" unique_id=1088637866 instance=ExtResource("6_mfdv2")]
visible = false

[node name="DebugScreen" parent="." unique_id=2045782823 instance=ExtResource("6_rvswv")]
process_priority = -1
visible = false

[node name="EnemyManager" type="Node" parent="." unique_id=705242850]
script = ExtResource("8_vef74")

[node name="LevelManager" type="Node" parent="." unique_id=1100285203]
script = ExtResource("8_j5wjh")

[node name="CrystalManager" parent="." unique_id=624430186 instance=ExtResource("9_vef74")]

[node name="GamePhaseManager" type="Node" parent="." unique_id=647316605]
script = ExtResource("13_80cx4")

[node name="WorldModuleManager" type="Node" parent="." unique_id=1892052256]
process_priority = -1
script = ExtResource("14_boyg6")

[node name="ScreenManager" type="Node" parent="." unique_id=1388008769]
process_mode = 3
process_priority = 1
script = ExtResource("15_e36ub")

[node name="PlayerManager" type="Node" parent="." unique_id=527546967]
script = ExtResource("17_x5p1p")

[node name="SpawnableManager" type="Node" parent="." unique_id=1175050760]
script = ExtResource("19_vaj68")

[node name="MouseInputManager" type="Node2D" parent="." unique_id=1359638225]
process_mode = 3
process_priority = -2
script = ExtResource("16_boyg6")

[node name="ExplosionEffectsLayer" type="CanvasLayer" parent="." unique_id=226302645]
script = ExtResource("11_mfdv2")

[node name="ScreenShakeLayer" type="CanvasLayer" parent="." unique_id=1986964170]

[node name="ScreenShake" type="ColorRect" parent="ScreenShakeLayer" unique_id=361886656]
material = SubResource("ShaderMaterial_lc2xo")
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
script = ExtResource("13_lc2xo")
metadata/_edit_lock_ = true

[node name="TransitionLayer" type="CanvasLayer" parent="." unique_id=804563990]
layer = 2

[node name="DebugLayer" type="CanvasLayer" parent="." unique_id=2028777421]
layer = 10

[node name="FpsCounter" type="Label" parent="DebugLayer" unique_id=1109875423]
offset_right = 40.0
offset_bottom = 102.0
script = ExtResource("8_215e1")

[node name="DebugDraw" type="Node2D" parent="DebugLayer" unique_id=1202650167]
process_mode = 3
process_priority = 10
script = ExtResource("19_boyg6")
